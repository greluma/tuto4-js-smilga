/**
 * * Truthy and Falsy
 * * Se usa para evaluar si una variable es verdadera o falsa*/
//! Valores Falsy: ``, '', "", 0, -0, Nan, false, null, undefined

let valor = ``;
if (valor) {
  console.log(`Truthy Valor`);
} else {
  console.log(`Falsy Valor`);
}
