/**
 * * Math object for maths operations
 */
/*const number = 12.176567;
const result = Math.floor(number); //to round numbers al menor*/

/*const number = 12.176567;
const result = Math.ceil(number); //to round numbers al mayor*/

/*const number = 12.176567;
const result = Math.trunc(number); //to round numbers a su parte integral*/

/*const number = 25;
const result = Math.sqrt(number); //para obtener la raíz cuadrada*/

/*const result = Math.PI; //para obtener PI*/

/*const result = Math.min(3, 6, 9, 8, 7, 4, 5, 2, 1,-1); //para obtener el mínimo*/
const result = Math.max(3, 6, 9, 8, 7, 4, 5, 2, 1, -1); //para obtener el mínimo

// const result = Math.random(); //para obtener un numero random entre 0 y 1
// const result = Math.ceil(Math.random() * 10); //para obtener un numero random de 1 dígitos != de 0
// const result = Math.ceil(Math.random() * 100); //para obtener un numero random de 2 dígitos
// const result = Math.ceil(Math.random() * 1000); //para obtener un numero random de 3 dígitos
// const result = Math.ceil(Math.random() * 10000); //para obtener un numero random de 4 dígitos
